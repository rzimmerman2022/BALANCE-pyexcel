# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
*.so
*.coverage
*.coverage.*
*.cache
*.hypothesis/
*.pytest_cache/
*.ruff_cache/
*.mypy_cache/
pip-log.txt
pip-delete-this-directory.txt
htmlcov/
dist/
build/
*.spec

# --- Virtual Environment ---
.env
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# --- Poetry ---
# poetry.lock # Usually committed, but keep this line commented out as we ARE committing it.
# pyproject.toml # DO NOT IGNORE

# --- Python in Excel & Jupyter ---
.ipynb_checkpoints/
.pyxlexcel/

# === Excel ===
# Ignore general temporary Excel files (often start with ~$)
~$*.xlsx
~$*.xlsm 

# === VS Code ===
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace
.history/

# === OS Specific ===
# Mac
.DS_Store
.AppleDouble
.LSOverride
# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
Desktop.ini

# === Data / Secrets / Logs ===
*.log
*.key
*.pem
*.pfx
secrets.yml
*.sqlite
*.db # Ignore database files unless explicitly needed
# Explicitly ignore the default CSV inbox if desired
# csv_inbox/
# Explicitly ignore sample data if large/private
# sample_data/

# === Project Specific Build/Output ===
# (Add any specific output files generated by your process if needed)
# output/ 

# === BALANCE-pyexcel Specific Ignores ===

# Ignore the LIVE workbook inside the workbook folder
# IMPORTANT: Make sure your TEMPLATE workbook (e.g., workbook/template/BALANCE-template-empty.xlsm) IS tracked by Git
workbook/BALANCE-pyexcel.xlsm

# Ignore ANY temporary Excel files created by the CLI in the workbook folder
workbook/*.temp.xlsx

# Ignore ANY lock files created by the CLI in the workbook folder
workbook/*.lock

# root copies live only in template folder
BALANCE-template.xlsm
ImportFromTemp.bas