feat(baseline_analyzer): add baseline_math skeleton with _clean_labels & stub build_baseline

Why:
• Sprint-5 Mini-Step 2 requires a foundational baseline_math engine to prepare for upcoming pattern-detection logic.

What:
• NEW src/baseline_analyzer/baseline_math.py  
  – _clean_labels(): standardises column names, removes header noise, canonicalises person aliases, tags source & row_id.  
  – build_baseline(): temporary stub that cleans expense+ledger frames, concatenates into audit_df, returns summary_df (per-person row count) plus audit_df.

• UPDATED src/baseline_analyzer/__init__.py  
  – Re-export build_baseline via __all__ and import side-effect.

Validation:
• Smoke-test (`python -c "...build_baseline..."`) confirms:
  ✅ module imports  
  ✅ data cleaning & concatenation  
  ✅ expected row counts (2 records in audit, 2 in summary).

Next:
• Mini-Step 3 will bolt on pattern detection & real math.
